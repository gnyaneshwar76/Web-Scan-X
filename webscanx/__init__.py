"""WebScanX — a universal security scanner.

Point it at a website, an API, your own source code, a mobile app, or a desktop
binary; it finds what's wrong, fixes the safe things it owns, flags the rest, and
puts everything into one report you can export in any format.

Phase 0 (this build) is the spine: the Finding schema, the report object, the
renderers, and the CLI. Scanners are added in later phases.
"""

__version__ = "0.1.0.dev0"
